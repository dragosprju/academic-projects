#include "../typedefs.h"
#include "../common_tools.h"
#include "free-ers.h"

extern inline void free_files(FILES* files);
extern inline void free_nodes(NODES* nodes);
extern inline void free_filepaths(char** filepaths, int no_files);
